name: Stage
on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  - uses: ./.github/workflows/_build.yaml
    with:
      target-env: pipeline
  - uses: ./.github/workflows/_test.yaml
  - uses: ./.github/workflows/_build.yaml
    with:
      target-env: stage
  - uses: ./.github/workflows/_deploy.yaml
    with:
      target-env: stage