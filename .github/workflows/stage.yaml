name: Stage
on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  - uses: ./.github/workflows/build.yaml
    with:
      target-env: pipeline
  - uses: ./.github/workflows/test.yaml
  - uses: ./.github/workflows/build.yaml
    with:
      target-env: stage
  - uses: ./.github/workflows/deploy.yaml
    with:
      target-env: stage