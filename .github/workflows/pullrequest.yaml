name: Pull Request
on:
  workflow_dispatch:
  pull_request:

jobs:
  test-build:
    uses: ./.github/workflows/build.yaml
    with:
      target-env: pipeline

  test:
    needs: test-build
    uses: ./.github/workflows/test.yaml